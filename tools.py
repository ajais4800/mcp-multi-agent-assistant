def create_task(title, due_date):
    return f"[MOCK] Task: {title}, due {due_date}"

def schedule_event(event, time):
    return f"[MOCK] Event: {event} at {time}"

def save_note(note):
    return f"[MOCK] Note saved: {note}"

def query_data(query):
    return f"[MOCK] Query executed: {query}"